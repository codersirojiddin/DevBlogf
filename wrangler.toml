# Cloudflare Pages configuration
# Docs: https://developers.cloudflare.com/pages/

name = "ither-blog"
pages_build_output_dir = "."

# Local dev: wrangler pages dev . --port 3000
# Variables below are for local dev only.
# Production variables go in Cloudflare Dashboard → Pages → Settings → Environment Variables

[vars]
# Override these in Cloudflare Dashboard for production!
SUPABASE_URL = ""
SUPABASE_ANON_KEY = ""
